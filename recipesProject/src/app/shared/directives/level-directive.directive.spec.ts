import { LevelDirectiveDirective } from './level-directive.directive';

describe('LevelDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LevelDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
